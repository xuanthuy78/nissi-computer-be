export type paginationTypes = {
  page: string;
  size: string;
  search: string;
};
export type orderItemTypes = {
  quantity: number;
  product: string;
};

export type userTypes = {
  name: string;
  email: string;
  password: string;
  phone: number;
  address: string;
};
